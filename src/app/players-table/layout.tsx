export default function PlayersTableLayout({
  children,
  // modal,
}: {
  children: React.ReactNode;
  // modal: React.ReactNode;
}) {
  return (
    <>
      {children}
      {/* {modal} */}
    </>
  );
}
